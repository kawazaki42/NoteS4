Local LLM
===

угадывает слова кароче

Large Language Model - он прям умный

он натренирован видеть вопрос и после него ответ

<!-- выбирает вариант с самой болшой вероятностью -->

выбирает на рандом с разной вероятностью

"статистический попугай"

принцип авторегрессии - кормим модели её же вывод

$O(n^2)$

базовэ болшэ языковэ модели: дают на вопрос еще список вопросов

инструкционная - шарит за вопрос

унимодальнэ - только текст

мультимодальнэ: видео, аудио, картинки

- даннэ обучения
    - напр. код скармливали?
    - кол-во параметров (весов)
        - это про связи а не обязательно про кол-во клеток
        - обычно: 0.1 - 1000 млрд
        - 3B (billion) - 3 миллиарда
        - размер веса
            - fp4
            - fp8
            - fp16
            - урезание называется _квантизация_
                - качество при этом не сильно падает
             
на самом деле мы работаем не со словами целиком, а с _токенами_

частое слово это один токен, иначе дробим

температура
---

отвечает за энтропию

и креативность

выбирает на рандом с разной вероятностью

эмпирическое правило: сколько модель весит, столько она возьмет на промежуточнэ даннэ и столько же для запуска

- LM Studio
- Jan
- `ollama`

- `localhost:11434`