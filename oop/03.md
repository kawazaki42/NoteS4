`std::vector` (`c++`) - динамический массив

вспоминаем шаблонные классы

ассоциативный массив (словарь) - для хранения пар ключ-значение

ключ может быть числом, а может например строкой

`pair` (`c++`)
- поля:
    - `first`
    - `second`

- `Kyky` -> `Kyky Epta!`
- `Ty kto?` -> `Ya bot!`

словарь имеет сложность O(1) или O(log n)

в отличие от массива, где O(n)

```cpp
#include <map>

map<int, float> m1;

m1[42] = 34.4;
m1[-5] = 444;
m1[0] = -55.01;
m1[452266] = -1.5;

cout << m1[0] << endl;  // -55.01

// добавит значение по умолчанию (у нас: 0.0)
cout << m1[5] << endl;
```

в python есть `dict`

```cpp
map<string, float> m2;
```

```cpp
const map<string, int> MONTH_DAYS = {
    {"december", 31}, {"january", 31}, {"february", 28},
    // ...
}
```

`map::at` кидает исключение? если не найден